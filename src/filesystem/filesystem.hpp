#ifndef FILESYSTEM_HPP_
#define FILESYSTEM_HPP_

#include "ch.hpp"

// TODO(yoos): Find and inherit some FAT filesystem
class FileSystem {
public:
  //FileSystem(SDCDriver *sdcd, SDCConfig *sdccfg);
};

#endif
